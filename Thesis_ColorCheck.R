colors <- c("#ffffcc", "#ffd27f", "#86b4b6", "#4a90e2", "#003366")
barplot(rep(1, length(colors)), col = colors, main = "Color Palette")

colors <- c("#ffffcc", "#ffd27f", "#a6a6d2", "#4a90e2", "#003366")
barplot(rep(1, length(colors)), col = colors, main = "Color Palette")

colors <- c("#ffffcc", "#ffcc8a", "#87a6b5", "#3e7fb1", "#003366")
barplot(rep(1, length(colors)), col = colors, main = "Color Palette")

